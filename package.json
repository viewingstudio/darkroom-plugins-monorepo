{
  "name": "darkroom-plugins-monorepo",
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter dev dev",
    "build": "pnpm -r --filter './packages/*' build",
    "clean": "pnpm -r clean",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "publish": "pnpm -r --filter './packages/*' build && pnpm publish:payload-access && pnpm publish:payload-deploy-trigger",
    "publish:payload-access": "cd packages/payload-access && npm publish --access public",
    "publish:payload-deploy-trigger": "cd packages/payload-deploy-trigger && npm publish --access public"
  },
  "devDependencies": {
    "@payloadcms/eslint-config": "3.28.0",
    "eslint": "^9.23.0",
    "prettier": "^3.4.2",
    "typescript": "catalog:"
  },
  "engines": {
    "node": "^18.20.2 || >=20.9.0",
    "pnpm": "^9 || ^10 || ^11"
  }
}
